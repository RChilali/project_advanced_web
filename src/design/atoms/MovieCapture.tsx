import styled from "@emotion/styled";

export const MovieCapture = styled("div")({
    display: "flex",
    width: "928px",
    height: "522px",
    borderRadius: "5px",
    backgroundColor: "black",
});