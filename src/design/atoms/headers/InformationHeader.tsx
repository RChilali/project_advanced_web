import styled from "@emotion/styled";

export const InformationHeader = styled("div")({
    display: "flex",
    justifyContent: "flex-start",
    alignItems: "flex-end",
    gap: "16px",
    flexShrink: 0,
    marginTop: "16px",
    marginBottom:"16px",
});