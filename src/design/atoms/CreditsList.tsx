import styled from "@emotion/styled";

export const CreditsList = styled("div")({
  display: "flex",
  gap: "25px",
  flexShrink: "0",
  flexDirection: "row",
  overflow: "auto",
  marginBottom: "16px",

});